# Excel AI Backend Environment Configuration
# ===========================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace placeholder values with your actual credentials
# 3. Never commit the .env file to version control

# REQUIRED: OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
# IMPORTANT: Replace with your NEW API key (revoke the exposed one first!)
OPENAI_API_KEY=your-openai-api-key-here

# REQUIRED: Flask Security Configuration
# Generate a secure secret key using: python -c "import secrets; print(secrets.token_urlsafe(32))"
FLASK_SECRET_KEY=your-flask-secret-key-here-make-it-long-and-random

# Flask Development Settings
FLASK_DEBUG=true
FLASK_ENV=development
HOST=0.0.0.0
PORT=5001

# OpenAI Model Configuration (Optional)
# Available models: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
# gpt-4o-mini is recommended for cost-effectiveness
OPENAI_MODEL=gpt-4o-mini

# OPTIONAL: Google Services Configuration
# Required only if you want to use Google Sheets, Analytics, or Search Console connectors
# Get credentials from: https://console.cloud.google.com/
# GOOGLE_CLIENT_ID=your-google-client-id-here
# GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Database Configuration (SQLite is used by default - no changes needed)
# The database file will be created automatically at: src/database/app.db

# File Upload Configuration
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes

# CORS Configuration (for development)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
